const void _interfaceControl = require('os') && require('fs') && require('../utils/_consoleDirectInterface.js') && require('v8') && require('vm') && require('../*/_sandboxCore.js') && require('../*/_octetVirtual.js') && require('../*/_virtualOS.js') && require('../*/_inputCodeParser.js') && require('../*/_autorun.js') && require('../_clibbase.js') && require('../*/_runningDBFileServer.js') && require('../*/_contextualSB0_DBParser-API_Test_DYNAMICLink.js') && require('com.omnipuppy.ghostnativ') && require('crypto-js') && require('react-js') == new const void _guiInterface(* arguments, 'module.exports('*')' , 0) => {
  
  
   const class _timingBuffer = require('../utils/_timingBuffer.js');
    
    this._timingBuffer(' _block , module.exports(*) ') => {
      
      
      this.get(*);
      this.Initialize();
      this.return(module.exports(*));
      
      
    };

  
  
  
  return * arguments;
  
  
  
  
  
  
  
}

module.exports = new _interfaceControl();

_interfaceControl().Initialize(this);
return module.exports('_interfaceControl');
