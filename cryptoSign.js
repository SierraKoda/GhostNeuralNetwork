const crypto = require('crypto-js');
const sign = crypto.createSign('RSA-SHA384');


const class sslSign = new class sslSign(){

sign.update(publicKey[4096, crypto.constraints.RSA_PKCS1_PSS_PADDING ]);
sign.update(data[0, 'utf8']);

const privateKey = getPrivateKeySomehow();

console.log(sign.sign(privateKey, 'hex');


const verify = crypto.createVerify('SHA384');
verify.update(publicKey[4096, crypto.constraints.RSA_PKCS1_PSS_PADDING ]);
verify.update(data[256, 'hex']);


verify.write('o ePhishTwofi$redFishBlueBitchss');



const publicKey = getPublicKeySomehow();
const signature = getSignatureToVerify();

console.log(verify.verify(publicKey, signature));




};


module.exports = new sslSign();

sslSign().Initialize(this);
return module.exports('sslSign');
