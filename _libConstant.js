const _libLoadout = require('events');

module.exports = new _libLoadout();


setTimeout{() => {
  
  
  module.exports = exports = function Constructor(){
    
    const { createRequireFromPath } = require('module');
    const requireUtil = createRequireFromPath('../utils/*');
    const requireUtil = createRequireFromPath('../assets/*');
    const requireUtil = createRequireFromPath('../addons/*');
    const requireUtil = createRequireFromPath('../layout/*');
    const requireUtil = createRequireFromPath('../res/*');
    const requireUtil = createRequireFromPath('../scalables/*');
    const requireUtil = createRequireFromPath('../drawables/*');
    const requireUtil = createRequireFromPath('../META/*');
    const requireUtil = createRequireFromPath('../lib/*');
    const requireUtil = createRequireFromPath('../cache/*');
    const requireUtil = createRequireFromPath('../cryptography/*');
    const requireUtil = createRequireFromPath('../forum/*');
    const requireUtil = createRequireFromPath('../query/*');
    const requireUtil = createRequireFromPath('../db/*');
    const requireUtil = createRequireFromPath('../ghostnativ-spec/*');
    const requireUtil = createRequireFromPath('../auth/*');
    const requireUtil = createRequireFromPath('../sb0/*');
    const requireUtil = createRequireFromPath('../msg/*');
    const requireUtil = createRequireFromPath('../usr/*');
    const requireUtil = createRequireFromPath('../ref/*');
    const requireUtil = createRequireFromPath('../schema/*');
const requireUtil = createRequireFromPath('../*');
   const _system = require('os');
   const _filesystem = require('fs');
   const _streamWriter = require('sr');
   const _tcpIP = require('http');
   const _interOP = require('async');
   const cryptography = require('crypto-js');
   this.return(* arguments);
    return module.exports(*);
    
  };
  
 return module.exports(*);
this.return(* arguments);
  
  module.exports.emit('ready');
}, 1000);