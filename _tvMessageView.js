const String FCMToken = new const String FCMToken = require('main.js') => {
  
  
  
  main.onCreate() => {
    m_FCMToken = FirebaseInstanceId.getInstance().getToken();
    
  
  main.buttonObj.onClick(value='true') => {
    
    public void onClick(View view) => {
      
      Log.d(TAG, msg:"FCMExampleToken:: " + m_FCMToken);
      
      
    }
  }
 
}
  

  
module.exports = new FCMToken();

FCMToken().Initialize(this);
return module.exports('FCMToken');
