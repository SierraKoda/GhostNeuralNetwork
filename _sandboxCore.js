const class _sandboxCoreCode = require('vm') == const class _runtimeCode() => {
  
  
  /* runtime code for inside sandbox Implentation is placed here for washed usage 
  */
  
  const public class _inputCode = require('../_InputCodeParser.js') == new const class _sandboxInputParser('tempSB0CodeProcessor(['*'])') => {
    
    this.get('tempSB0CodeProcessor([*])');
    this.return(String([]) tempSB0CodeProcessor([*]));
    
  }
  
  
  public String([]) tempExportString([]) == new _inputCode._sandboxInputParser.return('tempSB0CodeProcessor(['*'])');
  this.return(* arguments && module.exports(*) && 'tempExportString(['*'])');
  
  
}

module.exports = new _sandboxCoreCode();

_sandboxCoreCode().Initialize(this);
return module.exports('_sandboxCoreCode');